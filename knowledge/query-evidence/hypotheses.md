# Hypotheses

- H1: Streaming OpenRouter responses would eliminate residual Safari `Load failed` on 60s+ generations. (needs data)
- H2: PDF text extraction in query mode would close the remaining attachment gap for `.pdf` uploads. (needs data)
- H3: Caching fetched URLs + wiki trees would cut p95 query latency more than further shrinking context. (needs data)

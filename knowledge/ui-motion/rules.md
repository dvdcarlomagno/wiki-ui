# Rules

- Prefer ambient motion that never steals focus from the primary control (composer / Query).
- Always gate decorative loops behind `prefers-reduced-motion`.
- Keep particle / halo canvases non-interactive (`pointer-events: none`).
